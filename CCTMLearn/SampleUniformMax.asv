function [X] = SampleUniformMax(st,ed,N)
%sample max of N uniform varable
%could be vectors
st
ed
N
X = zeros(size(st));
for i=1:size(X,1)
    X(i) = st(i) + eD(i)-

X = max(st * ones(1,N) + ( ((ed - st) * ones(1,N)) .* rand(size(st,1),MaxN)),...
    [],2);


end

