function [mu,Sigma] = NPNFit(Eta)
%fit NPN on Eta (D \times K), Eta is the sample mean
% no need to fit the marginal cdf as it is ecdf of Eta.


EtaMean = mean(Eta,2);
EtaStd = std(Eta,0,2);

F = zeros(size(Eta));
[m,k]=size(F);
for i=1:m
    for j=1:k
       F(i,j) = TruncECdf(Eta(:,j),m,Eta(i,j));
    end


end

